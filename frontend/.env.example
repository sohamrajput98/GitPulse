# Frontend Configuration
# ---------------------

# This token is used by the React app to make direct requests to the GitHub API.
# It MUST be prefixed with VITE_ to be exposed to the client-side code.
VITE_GITHUB_API_TOKEN=""